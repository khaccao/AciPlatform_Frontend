# Môi trường Local (Sử dụng qua Proxy của Vite để tránh lỗi CORS)
VITE_API_URL=/api

# Để dùng trực tiếp tới Server (Phải đảm bảo Server hỗ trợ CORS hoặc qua Nginx)
# VITE_API_URL=https://sit.backend.nguyenbinh.info.vn/api
